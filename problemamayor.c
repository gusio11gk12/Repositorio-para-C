/*
 Autor: Gustavo Garcia Barrera
 */

/*Realizar un código que determine si el usuario esmayor o menor de edad dependiendo del año de nacimiento
 
#include <stdio.h>

int main(){//Inicio metodo principal
 
        //Declarcion de variables
	int fecha_nacimiento;
	int fecha_actual;
	int diferencia= 0;

	printf ("Digite su año de nacimiento; ")
	scanf ("%d", &fecha_Nacimiento);

	diferencia = (fecha_Actual - fecha_Nacimineto);
	
	if (diferencia >= 18){
	           printf("%s %d %s", "El usuario tiene", diferencia, "por lo tanto es mayor de edad");
	 }
        else {
	           printf("%s %d %s", "El usuario tiene", diferencia, "por lo que es menor de edad");
	}
}//fin metodo principal

